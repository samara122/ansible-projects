- name: Install dependencies (prerequisites)
  yum:
    name: "{{ packs }}"
    state: latest
  vars:
    packs:
      - epel-release
      - yum-utils
      - http://rpms.remirepo.net/enterprise/remi-release-7.rpm 